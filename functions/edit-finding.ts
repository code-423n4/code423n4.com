// wip: endpoint for self-serve submission editing

export async function handler(event) {
  // event.httpMethod
  //   GET: load finding(s)
  //   POST: write finding

  return {
    statusCode: 200,
    body: "Some Response",
  };
};
